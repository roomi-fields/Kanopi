// p5 fixture (migré de p5.kanopi) — voix-code p5 → vidéo.
core

actor sketch  eval.p5
-----

S -> sketch_r

sketch_r -> sketch.`function setup() {
  createCanvas(400, 400);
  noStroke();
}
function draw() {
  background(40, 90, 180);
  fill(255, 200, 50);
  ellipse(200, 200, 200, 200);
}`
