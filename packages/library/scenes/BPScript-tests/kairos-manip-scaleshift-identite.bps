// @language: bpscript
// kairos-manip-scaleshift-identite.bps — sert src/projection/rotate-keyxpand-e2e.test.ts (kairos,
// sortie du BPScript en dur 2026-08-09). C4 avec scaleshift:0 : preuve que le contrôle à zéro degré
// est une IDENTITÉ (aucune mutation de hauteur) plutôt qu'un cas particulier non résolu.

@core
@controls
@alphabet.western:midi

S -> C4(scaleshift:0)
