// @language: bpscript
// kairos-manip-transpose-global-migre.bps — sert src/projection/transpose-digital-e2e.test.ts
// (kairos, sortie du BPScript en dur 2026-08-09). @transpose:-1200c au niveau scène (forme MIGRÉE,
// cents) sur deux notes C4 D4 : preuve que CHAQUE note tombe à hz/2, noms préservés (cascade + cadre).

@core
@alphabet.western:midi
@transpose:-1200c

-----
S -> C4 D4
