// checkHomo.bps — migré de bp3-engine/test-data/-gr.checkHomo (porter bp3ToScene, 2026-07-17)
// Round-trip fidèle (règles = -gr, modulo commentaires/espacement flags) ; AST BPx valide.
// Homomorphisme INLINE du .gr d'origine (-ho.checkHomo) chargé via la table lib (sections *,H,TR) :
// TOUT homo PEUPLE la table (décision 2026-07-18) — BPx porte opaque, Kairos résout. Sans, les
// marqueurs *,TR fuiraient non résolus (sortie fausse). Byte-id BP3 legacy intact (@transcription = BPx-only).
@transcription.checkhomo

S -> $X * TR &X Y
X -> a b
Y -> ${a b} TR * &{b' b}
