// @language: bpscript
// kairos-trace-controle-suffixe-seul.bps — sert src/trace/vue-trace.test.ts (kairos,
// CONTRE-SIGNATURE [499] — mon résolveur NE dépend PAS de slotIndex côté règle). Un seul contrôle
// suffixé (`transpose:-1200c`) sur la production : sert de terrain à la mutation de `slotIndex` qui
// prouve que la résolution du libellé est insensible à cette valeur.

@core
@alphabet.western:midi

-----
S -> C4(transpose:-1200c) D4
