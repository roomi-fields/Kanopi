// @language: bpscript
// kairos-octave-transpose-global-partoken.bps — sert src/projection/transpose-global-trnoop.test.ts
// (kairos, TR-NOOP étage 1). Deux notes portant CHACUNE un suffixe `(transpose:-1200c)` per-token :
// preuve que la valeur cascade en chaîne brute verbatim jusqu'à leaf.controles.

core
alphabet.western:midi

-----
S -> C4(transpose:-1200c) D4(transpose:-1200c)
