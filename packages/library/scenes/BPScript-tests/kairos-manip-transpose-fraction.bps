// @language: bpscript
// kairos-manip-transpose-fraction.bps — sert src/projection/transpose-digital-e2e.test.ts (kairos,
// sortie du BPScript en dur 2026-08-09). C4 avec transpose:3/2 (intervalle NU, format fraction) :
// preuve que la chaîne brute "3/2" traverse verbatim jusqu'au controlTable et que MA coercition
// interval-ratio résout exactement hz × 1.5, nom et registre préservés (cadre déplacé, corps
// frameRatio réel). Sert aussi le cas SANS digitalLib (la résolution branchée crie au chargement).

@core
@alphabet.western:midi

S -> C4(transpose:3/2)
