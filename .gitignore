node_modules/
dist/
build/
.vite/
*.log
.DS_Store
.env
.env.local
target/
src-tauri/target/

# RTFM local database
.rtfm/

# Playwright MCP artefacts
.playwright-mcp/

# Playwright test artefacts (screenshots, traces) — regenerated on each run
test-results/
playwright-report/

# Strategic/internal docs — not for public repo
docs/plan/

# Local debug screenshots archive (not for public repo)
_archive/
packages/ui/.playwright-artifacts/
.aider*

# Rapport de la passe de bancs — artefact lu par `scripts/garde-population-bancs.mjs`,
# regénéré à chaque passe. Ce qui se VERSIONNE est le registre attendu (`scripts/bancs-attendus.json`),
# jamais la photo du moment : commiter la photo ferait coïncider les deux par construction, et le
# garde ne mordrait plus jamais.
packages/ui/.vitest-rapport.json
